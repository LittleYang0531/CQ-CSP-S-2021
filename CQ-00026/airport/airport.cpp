#include<iostream>
#include<algorithm>
#include<cstring>
using namespace std;
int main(){
freopen("airport.in","r",stdin);
freopen("airport.out","w",stdout);
	int n, m, x, a, e;//a代表一共停靠了多少飞机，m国内停靠班机 
	//scanf("%lld%lld%lld", &n, &m, &x);
	for(int e = 0; x <= n; x++);//e代表的是国内航班廊桥 y则代表的是国际航班廊桥 
	for(int y = n; y <= n; y--);//每一次的廊桥安排

	return 0; 
}
