#include <cstdio>
#include <algorithm>
#include <iostream>
using namespace std;

int main() {
	freopen("traffic.in", "r", stdin);
	freopen("traffic.out", "w", stdout);
	int a, b, Case;
	scanf("%d %d %d", &a, &b, &Case);
	while(Case -- ) {
		puts("0");
	}
	return 0;
}
